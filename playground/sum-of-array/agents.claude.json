{
  "version": "1.0",
  "agents": {
    "architect": {
      "executor": "claude_cli",
      "command_template": ["claude", "-p", "{prompt}"],
      "prompt_template": "Act as the architect agent. Follow the instructions in {instruction}. Input artifacts: {inputs}. Write outputs to: {outputs}. Repos: {repos}.",
      "context_window": "isolated"
    },
    "reviewer": {
      "executor": "claude_cli",
      "command_template": ["claude", "-p", "{prompt}"],
      "prompt_template": "Act as the reviewer agent. Follow the instructions in {instruction}. Input artifacts: {inputs}. Write outputs to: {outputs}. Repos: {repos}.",
      "context_window": "isolated"
    },
    "developer": {
      "executor": "claude_cli",
      "command_template": ["claude", "-p", "{prompt}"],
      "prompt_template": "Act as the developer agent. Follow the instructions in {instruction}. Input artifacts: {inputs}. Write outputs to: {outputs}. Repos: {repos}.",
      "context_window": "isolated"
    },
    "test-writer": {
      "executor": "claude_cli",
      "command_template": ["claude", "-p", "{prompt}"],
      "prompt_template": "Act as the test-writer agent. Follow the instructions in {instruction}. Input artifacts: {inputs}. Write outputs to: {outputs}. Repos: {repos}.",
      "context_window": "isolated"
    },
    "integrator": {
      "executor": "claude_cli",
      "command_template": ["claude", "-p", "{prompt}"],
      "prompt_template": "Act as the integrator agent. Follow the instructions in {instruction}. Input artifacts: {inputs}. Write outputs to: {outputs}. Repos: {repos}.",
      "context_window": "isolated"
    }
  }
}
