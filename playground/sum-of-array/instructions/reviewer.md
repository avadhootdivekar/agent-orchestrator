# Reviewer — Sum of Array (Task Review)

## Task

Review the implementation and tests for task `t1`.

## Inputs

- `output/tasks/t1/solution.py` — the Python implementation
- `output/tasks/t1/test_solution.py` — the pytest test file

## Output

Write your review to `output/tasks/t1/review.md`.

## Check list

- Does `solution.py` define `sum_array(nums: list[int]) -> int`?
- Does `test_solution.py` cover all required cases (empty, positives, negatives, mixed)?
- Run the tests to confirm they pass: `python -m pytest output/tasks/t1/test_solution.py -v`
- Is the code correct, idiomatic Python?

Write a brief pass/fail verdict with one-line rationale. Include the pytest output summary.
