# Instruction: implement

Payload referenced by path. Read the design at the provided input path, implement the change in the
`core` repo, and write a short implementation report to the declared output (`output/impl-report.md`).
