# Implement: count_vowels

`textutils.py` declares `count_vowels(s)` but it is not implemented yet (it raises
`NotImplementedError`).

Implement `count_vowels(s: str) -> int` in `textutils.py` so it returns the number of
vowel characters (`a`, `e`, `i`, `o`, `u`, case-insensitive) in `s`. Non-letter
characters and consonants do not count.

Make `tests/test_textutils.py` pass. Do not modify `tests/test_textutils.py`.
