# Implement: is_leap_year

`calendarutils.py` declares `is_leap_year(year)` but it is not implemented yet (it
raises `NotImplementedError`).

Implement `is_leap_year(year: int) -> bool` in `calendarutils.py` using the standard
Gregorian rule: a year is a leap year if it is divisible by 4, except centurial years
(divisible by 100) which are leap years only if also divisible by 400.

Make `tests/test_calendarutils.py` pass. Do not modify `tests/test_calendarutils.py`.
