# Instruction: review (`ao run --dry-run` worked example)

Payload referenced by path. Read the test report at the provided input path. Review the
CLI/docs/test artifacts produced by this workflow for consistency (does the documented flag
behavior match the implementation report; do the tests cover the stated `emit_tasks`
limitation). Write a short review verdict to the declared output path.
