# Architect Design — Sum of Array

## Task

Produce a concise design document for implementing `sum(nums: list[int]) -> int`.

## Output

Write your design to `output/design.md`.

Include:
- Function signature
- Algorithm description (one-sentence: iterate and accumulate)
- Edge cases: empty list returns 0, negatives handled correctly
- No external dependencies needed

Keep it brief. This is a trivial function; the design should be a single short section.
