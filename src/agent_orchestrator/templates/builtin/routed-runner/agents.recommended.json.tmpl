{
  "_note": "Generated by the routed-runner ao workflow template (see docs-md/template-cost-hygiene-hld.md and this template's README.md 'Recommended agent command_template hygiene' section). This is a MERGE REFERENCE, not a live config -- `ao` never reads this file directly. Copy the `extra_args` you want from `agents` below into your own agents.json (the file your .ao/config.yaml `agents:` field, or AO_AGENTS, points at). Written once, the first time `ao new routed-runner ...` runs in a fresh workspace (keep_existing) -- later `ao new` calls never overwrite it, so any local edits you make here are preserved. If you want the template's current recommendation instead of what you have, delete this file and re-run `ao new`.",
  "_autocompact_defaults": {
    "architecture_roles": "500000",
    "dev_cycle_roles": "180000"
  },
  "_autocompact_rationale": "docs-md/template-cost-hygiene-hld.md, section 3.4 (Rev 2 -- role split)",
  "_seeded_from_template_version": "1.0",
  "version": "1.0",
  "agents": {
    "architect": {
      "executor": "claude_cli",
      "extra_args": ["--autocompact", "500000"]
    },
    "architect-opus": {
      "executor": "claude_cli",
      "extra_args": ["--autocompact", "500000"]
    },
    "reviewer-opus": {
      "executor": "claude_cli",
      "extra_args": ["--autocompact", "500000"]
    },
    "developer": {
      "executor": "claude_cli",
      "extra_args": ["--autocompact", "180000"]
    },
    "full-tester": {
      "executor": "claude_cli",
      "extra_args": ["--autocompact", "180000"]
    },
    "manager": {
      "executor": "claude_cli",
      "extra_args": ["--autocompact", "180000"]
    },
    "market-surveyor": {
      "executor": "claude_cli",
      "extra_args": ["--autocompact", "180000"]
    },
    "reviewer": {
      "executor": "claude_cli",
      "extra_args": ["--autocompact", "180000"]
    },
    "tester": {
      "executor": "claude_cli",
      "extra_args": ["--autocompact", "180000"]
    }
  }
}
